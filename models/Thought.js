const {
    Schema,
    model
} = require('mongoose');

const SocialThought = new Schema({
    thoughtText: {
        type: String,
        required: true,
    }
})
thoughtText 
createdAt 
username 
reactions 